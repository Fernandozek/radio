import React from 'react';
import {MenuProps} from '../../components/Header/Container';
import PageTemplate from '../PageTemplate';

const Album3: React.FC<MenuProps> = (props) => {
    return(
        <PageTemplate
            topBar={true}
        >
            
        </PageTemplate>
    );
}

export default Album3;